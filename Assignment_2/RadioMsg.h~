#ifndef RADIOFLOODINGMSG_H
#define RADIOFLOODINGMSG_H

enum {
	AM_FLOODING_MSG = 6,
	TIMER_PERIOD_MILLI = 1000 
};

typedef nx_struct radio_flooding_msg {
	nx_uint8_t id;
	nx_uint8_t type;
	nx_uint8_t nodeid;
	nx_uint16_t data;
	//nx_bool forwardORnot;
} radio_flooding_msg_t;

#endif
